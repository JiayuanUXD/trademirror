# 🚀 TradeMirror · Claude Code 启动包

> 用 Claude Code 从零构建交易日志Web应用的完整Prompt组合。
> 设计原则：**不是一个长提示词，而是结构化的项目启动包**。

---

## 📋 使用说明

本启动包包含 **3 个核心文件**，需要在 Claude Code 中按顺序使用：

```
1. CLAUDE.md           → 放在项目根目录，Claude Code 会自动加载
2. 首次对话主提示词    → 第一次启动 Claude Code 时粘贴
3. 分阶段任务清单      → 按阶段逐步推进，每个阶段一次对话
```

**前置准备**：
1. 安装 Claude Code：`npm install -g @anthropic-ai/claude-code`
2. 创建项目目录：`mkdir trade-mirror && cd trade-mirror`
3. 把 PRD 文件（`PRD_交易日志Web应用_TradeMirror.md`）放在项目根目录
4. 启动 Claude Code：`claude`

---

## 📄 文件1：CLAUDE.md（项目宪章）

> 把下面的内容保存为项目根目录的 `CLAUDE.md`。这是 Claude Code 每次启动会自动加载的项目说明。

```markdown
# TradeMirror · 项目宪章

## 项目背景

TradeMirror（交易之镜）是一个面向中国A股个人投资者的交易日志Web应用。
核心定位：**不帮用户预测市场，帮用户看清自己**。

完整产品需求见 @PRD_交易日志Web应用_TradeMirror.md

## 用户画像

主要用户：38岁IT工程师，本金80-150万，6年A股投资，情绪化交易困扰严重。
他不需要又一个炒股软件，他需要一面照见自己交易模式的镜子。

## 核心原则（最重要的5条）

1. **简单胜过复杂**：MVP阶段不引入任何不必要的依赖、不过度抽象
2. **移动端优先**：60%使用场景在手机上，所有页面必须移动端友好
3. **数据本地优先**：用户敏感的交易数据，本地存储优先于云端
4. **不连接券商交易接口**：仅记录用途，避免任何"代客交易"嫌疑
5. **不提供荐股或交易建议**：UI/文案必须清晰，这是日志工具不是投顾

## 技术栈

### 前端
- **框架**: Next.js 14+ (App Router)
- **语言**: TypeScript（严格模式）
- **样式**: Tailwind CSS + Shadcn/ui
- **状态管理**: Zustand
- **图表**: Recharts（基础）+ ECharts（K线/复杂图表）
- **表单**: React Hook Form + Zod
- **日期**: dayjs

### 后端
- **运行时**: Node.js 20+
- **框架**: Next.js API Routes（MVP阶段无需单独后端服务）
- **ORM**: Drizzle ORM
- **数据库**: PostgreSQL（生产）/ SQLite（本地开发）
- **认证**: NextAuth.js（v5）

### 开发工具
- **包管理**: pnpm
- **代码规范**: ESLint + Prettier
- **Git Hooks**: husky + lint-staged
- **测试**: Vitest（单元）+ Playwright（E2E，V1.5后引入）

## 代码风格

### TypeScript
- 严格模式（`strict: true`）
- 不使用 `any`，必要时用 `unknown` + 类型守卫
- 优先使用 `type` 而非 `interface`（除非需要继承）
- 导出函数必须有显式返回类型

### React/Next.js
- 默认 Server Components，按需 `"use client"`
- 文件命名：kebab-case（如 `decision-card.tsx`）
- 组件命名：PascalCase（如 `DecisionCard`）
- 组件目录结构：`components/{feature}/{component-name}.tsx`

### 样式
- 优先用 Tailwind 工具类
- 颜色用 CSS 变量（在 `globals.css` 定义主题）
- 避免内联 style，避免 styled-components
- 移动端：所有页面在 375px 宽度下不能横向滚动

### 导入顺序
1. React/Next.js 内置
2. 第三方库
3. 项目内部模块（按层级：lib → hooks → components → types）
4. 类型导入（用 `import type`）

## 项目目录结构

```
trade-mirror/
├── app/                      # Next.js App Router
│   ├── (auth)/              # 认证相关路由
│   ├── (dashboard)/         # 主应用路由组
│   │   ├── decisions/       # 决策卡模块
│   │   ├── holdings/        # 持仓库模块
│   │   ├── reviews/         # 周/月复盘
│   │   ├── analytics/       # 分析仪表盘
│   │   └── settings/        # 设置
│   ├── api/                 # API Routes
│   └── globals.css
├── components/
│   ├── ui/                  # Shadcn 基础组件
│   ├── decisions/           # 决策卡相关组件
│   ├── holdings/            # 持仓相关组件
│   └── shared/              # 跨模块组件
├── lib/
│   ├── db/                  # 数据库（schema + 查询）
│   ├── utils/               # 工具函数
│   ├── analytics/           # 分析算法
│   └── validators/          # Zod schemas
├── hooks/                   # React Hooks
├── stores/                  # Zustand stores
├── types/                   # 全局 TypeScript 类型
└── public/                  # 静态资源
```

## 关键业务规则（必须遵守）

### 决策卡（Decision Card）
- **不允许删除**：错误不能被掩盖，只能"归档"
- **不允许修改情绪评分**：填了就锁定，避免事后美化
- **必填字段**：股票、方向、价格、数量、理由、决策依据、3项情绪评分、止损价
- **危险信号自动计算**：FOMO≥7 / 平静度≤4 / 不符合体系 / 含非理性决策依据

### 持仓档案
- **每只股票一份档案**
- **逻辑评分0-10**：根据理由数量、可验证性、财务数据完整度
- **撤退条件可checkbox打勾**：触发即推送预警

### 周/月复盘
- **不能跳过**：上周复盘未完成，不能开始新一周的决策卡
- **三问必填**：本周最对的事、最错的事、重来一次会怎样

## 开发工作流

### 每次开始新功能前
1. 阅读 @PRD_交易日志Web应用_TradeMirror.md 中对应模块
2. 在 `docs/decisions/` 创建一份 ADR（架构决策记录）
3. 先写类型定义和 Zod schema
4. 再写数据层（DB schema + query）
5. 再写 UI 组件
6. 最后接入页面

### Bug 修复
- 不要"看起来修好了"就提交
- 必须能解释清楚 root cause
- 必须有单元测试覆盖（除非是纯UI bug）

### Git Commit 规范
- 用约定式提交（Conventional Commits）
- 类型：feat / fix / docs / style / refactor / test / chore
- 例：`feat(decisions): add danger signal calculation`

## 性能要求

- 首屏加载 < 2s（Lighthouse Performance > 90）
- API 响应 P95 < 200ms
- 数据库查询必须有索引规划，不允许全表扫描

## 安全要求

- 所有用户输入必须 Zod 校验
- 数据库查询使用参数化（Drizzle 自动处理）
- 密码用 bcrypt（cost ≥ 12）
- 敏感数据（如账户金额）传输用 HTTPS（本地开发除外）
- 不要把 .env 文件提交到 git

## 我喜欢什么/不喜欢什么

### 喜欢
- 简洁的代码，函数 < 50 行
- 类型严谨，让编译器替我抓 bug
- 完整的注释，特别是"为什么这么做"
- 渐进增强，先跑起来再优化

### 不喜欢
- 过度设计（YAGNI 原则）
- 引入大型库解决小问题（如为了发个请求引入 axios）
- 默认配置一大堆魔法，需要查文档才能理解
- 抽象层数过多（最多3层）
- 任何形式的 "TODO: implement later"（要么做，要么不做，不留烂尾）

## 调试与提问

当你不确定时：
- 不要瞎猜，先问我
- 提供 2-3 个选项让我选，不要给一个"最佳方案"
- 重要的架构决策（数据库 schema、状态管理结构）必须先讨论再实施

当你完成一个功能时：
- 告诉我做了什么（不超过5条）
- 告诉我没做什么（明确scope）
- 告诉我下一步建议

当你犯错时：
- 直接说"我错了"，不要绕弯子
- 解释错在哪
- 说怎么避免再犯
```

---

## 📄 文件2：首次对话主提示词

> 把下面这段话作为你在 Claude Code 中的**第一条消息**。

```
你好。我想用你帮我构建一个完整的Web应用，项目代号 TradeMirror。

【项目背景】
我是一名6年的A股投资者，深受情绪化交易困扰。我设计了一个面向同类用户的
交易日志Web应用 TradeMirror，核心定位是"用数据看清自己的交易模式"。

【完整文档】
请先按顺序阅读项目根目录下的两份核心文档：

1. @CLAUDE.md - 项目宪章（技术栈、代码规范、工作流）
2. @PRD_交易日志Web应用_TradeMirror.md - 完整产品需求文档

读完之后，请用你自己的话向我复述：
1. 这个产品的核心定位是什么（用一句话）
2. 它的核心模块有哪些（列出名称即可）
3. 与 TraderWaves 等同类产品最大的差异是什么
4. 你认为开发过程中最大的技术挑战会是什么（说3个）
5. 你对MVP阶段（前3个月）的范围建议是什么

【重要约定】
- 在我们开始写代码之前，先完成上述5个问题的回答
- 然后我会跟你说"开始第1阶段"，我们再进入具体的开发
- 整个项目按阶段推进，每个阶段聚焦一个目标，不贪多
- 任何架构决策都要先讨论，不要直接动手
- 如果你不确定某个技术选型，提供2-3个选项给我选

【你的工作方式】
- 简洁、直接、不啰嗦
- 关键决策前先给我看方案
- 不要为了"看起来在工作"而生成大量代码
- 遇到歧义先问我，不要瞎猜
- 完成任务后报告：做了什么、没做什么、下一步建议

现在开始吧——先读文档，再回答那5个问题。
```

---

## 📄 文件3：分阶段任务清单（每个阶段单独一次对话）

> 完成首次对话后，按下面的阶段顺序逐步推进。**每个阶段建议单独开一个新的 Claude Code 会话**（用 `/clear` 清空上下文），避免上下文过长导致质量下降。

### 🎯 阶段0：项目初始化（预计1天）

**目标**：搭好骨架，能跑起来。

```
开始阶段0：项目初始化。

任务清单（按顺序完成，每完成一项告诉我）：

1. 初始化 Next.js 14 项目
   - 使用 TypeScript、Tailwind、App Router
   - 包管理用 pnpm
   - 配置 ESLint + Prettier + husky

2. 安装核心依赖
   - shadcn/ui（初始化并安装：button, input, card, dialog, form, select, tabs）
   - zustand
   - react-hook-form + zod
   - dayjs
   - drizzle-orm + drizzle-kit
   - better-sqlite3（本地开发）+ postgres（生产）
   - lucide-react（图标）

3. 创建项目目录结构
   - 严格按照 CLAUDE.md 里的目录结构创建
   - 在每个主目录下创建 README.md 简单说明用途

4. 配置主题
   - 主色 #1F4E78，辅色 #00C49A，警示 #FF6B35
   - 设置中文字体栈（系统字体优先，备选 Inter）
   - 在 globals.css 配置好 CSS 变量

5. 创建一个最简单的首页
   - 展示 "TradeMirror · 你的交易之镜"
   - 一个 CTA 按钮 "开始第一张决策卡"（先不实现跳转）
   - 移动端友好（375px 宽度无横向滚动）

6. 创建文档目录
   - docs/decisions/ （ADR 架构决策记录）
   - docs/decisions/README.md 说明 ADR 写法
   - 创建 ADR 0001：Why Next.js + Drizzle + SQLite

完成后告诉我：
- 项目能跑起来吗？（pnpm dev 后看到首页）
- 有哪些决策你不确定，希望我确认？

注意：这一阶段不写任何业务逻辑。
```

### 🎯 阶段1：数据库设计与决策卡 MVP（预计3-5天）

**目标**：实现核心模块——决策卡的创建、列表、详情。

```
开始阶段1：决策卡 MVP。

【目标】
让用户能够创建一张决策卡、查看决策卡列表、查看单张详情。
这是整个产品最核心的功能，必须打磨到位。

【任务】

1. 数据库设计
   先不要写代码，先讨论：
   - 用户表（users）：MVP 阶段是否需要多用户？还是先做单用户本地版？
   - 决策卡表（decision_cards）：所有字段（参考 PRD 模块3.2.4）
   - 持仓表（holdings）：先建表，关联关系预留
   - 错误类型表（error_types）：先建表，预留

   给我看你设计的 schema（Drizzle 写法），我确认后再 migrate。

2. 类型定义与 Zod schema
   在 lib/validators/decision-card.ts 创建：
   - DecisionCardSchema（Zod）
   - DecisionBasisEnum
   - DangerSignal 计算函数（重点：必须有单元测试）

3. 数据层
   在 lib/db/queries/decision-cards.ts 创建：
   - createDecisionCard
   - listDecisionCards (支持分页 + 过滤)
   - getDecisionCardById
   - 暂不实现 update/delete（业务规则：决策卡不允许删除/修改）

4. API Routes
   - POST /api/decisions      创建决策卡
   - GET  /api/decisions      列表
   - GET  /api/decisions/:id  详情
   不要做 PUT/DELETE

5. UI 组件
   按 PRD 3.2.1 的 3 屏流程实现：
   - 屏1：基础信息（股票、方向、价格、数量、理由）
   - 屏2：决策依据 + 情绪三维评分（滑块）
   - 屏3：风控（止损、符合度）
   
   组件位置：components/decisions/decision-card-form.tsx
   每屏一个 step 组件，用 Tabs 或 multi-step form 实现

6. 决策卡列表页
   - 路由：app/(dashboard)/decisions/page.tsx
   - 卡片式展示
   - 顶部"+"按钮，点击进入创建流程
   - 危险信号用颜色标识（红/黄/绿）

7. 决策卡详情页
   - 路由：app/(dashboard)/decisions/[id]/page.tsx
   - 完整展示所有字段
   - 1周/1月后回填股价的功能（输入框 + 保存）
   - 危险信号 + 事后复盘区域

【边界】
- 不实现智能拦截（拦截弹窗）：阶段3再做
- 不实现移动止盈条件单：阶段4再做
- 不实现券商导入：V1.5 再做
- 暂用 mock 行情数据（写在 lib/mock-prices.ts）

【完成标准】
- 我能在手机上完成一张决策卡的创建
- 我能在列表页看到所有决策卡，能点进去看详情
- 危险信号能正确计算和显示
- 单元测试覆盖 dangerSignal 函数

完成后告诉我：
- 跑起来后我应该测试哪几个核心流程？
- 哪些地方你做了取舍，需要后续优化？
```

### 🎯 阶段2：持仓与逻辑库（预计3天）

```
开始阶段2：持仓与逻辑库（PRD 模块3.3）。

【目标】
实现持仓的"成长档案"——每只股票一个独立的逻辑页面。

【任务】
1. 数据库 schema：holdings 表 + holding_logics 表 + exit_conditions 表
2. 持仓总览页（app/(dashboard)/holdings/page.tsx）
   - 卡片式列表，展示成本/现价/浮盈/仓位/健康分
   - 状态过滤（持有中/观察中/已清仓）
3. 单股详情页 - 5个Tab：
   - 持有逻辑（理由、护城河、财务数据、逻辑评分）
   - 持有前提（checklist）
   - 撤退条件（设置 + checklist）
   - 操作记录（关联该股的所有决策卡）
   - 新闻追踪（手动添加）

4. 关联功能：
   - 决策卡 schema 增加 holdingId 字段（外键）
   - 创建决策卡时可选择关联到某个持仓
   - 持仓详情页的"操作记录"Tab 自动汇总该股的决策卡

5. 健康分计算算法（在 lib/analytics/holding-health.ts）
   - 逻辑完整度（30%）：理由数量 + 财务数据完整度
   - 撤退条件设置率（30%）：是否设了止损、业绩条件等
   - 操作纪律（40%）：该股的决策卡是否多次出现危险信号

【边界】
- 不实现新闻自动抓取，只支持手动添加
- 不实现移动止盈条件单

完成后告诉我健康分算法的具体公式，我review。
```

### 🎯 阶段3：周复盘 + 智能拦截（预计4天）

```
开始阶段3：周复盘 + 智能拦截。

这一阶段实现两个功能：
A. 周复盘模块（PRD 模块3.4）
B. 决策卡的"智能拦截"机制（PRD 模块3.2.2）

【A. 周复盘】
1. 数据库：weekly_reviews 表
2. 路由：app/(dashboard)/reviews/weekly/
3. 自动汇总（用户进入页面就能看到）
   - 本周账户走势（暂用手填的周初/周末资产）
   - 本周决策卡卡片化展示
   - 本周高危交易统计
4. 三问必填表单
5. 纪律分7项打分（部分自动计算）
6. 系统智能评估（在 lib/analytics/weekly-evaluation.ts）

【B. 智能拦截】
1. 在决策卡提交前增加预检查（lib/analytics/danger-check.ts）
2. 触发条件（5种，见 PRD 模块3.2.2 表格）
3. 拦截弹窗组件（components/decisions/danger-dialog.tsx）
   - 必须显示用户自己的历史数据（不是泛泛警告）
   - 例："你过去 FOMO≥7 的 X 次操作中 Y 次亏损"
4. 用户必须点"我知道了，继续"或"取消"

【关键】
拦截弹窗的查询要高效，不能让提交决策卡变慢。
SQL 查询要加合适的索引。

完成后告诉我：
- 拦截触发的 5 种条件你都实现了吗？
- 历史数据查询的性能如何？
```

### 🎯 阶段4：分析仪表盘（预计5天）

```
开始阶段4：分析仪表盘（PRD 模块3.6）。

【目标】
实现 7 张核心图表，让数据揭穿用户的交易幻觉。

【任务】
1. 路由：app/(dashboard)/analytics/
2. 实现 7 张图（按优先级）：
   - P0：图1 盈亏归因瀑布图（按决策依据拆解）
   - P0：图2 情绪评分 vs 实际盈亏 散点图
   - P0：图6 理性 vs 非理性决策对比
   - P1：图3 纪律分 vs 周收益 趋势图
   - P1：图7 账户净值曲线 vs 大盘（大盘数据用 mock）
   - P2：图4 错误重复频率热力图
   - P2：图5 单股操作复盘图（叠加K线）

3. 每张图必须有：
   - 标题
   - 数据图表（用 Recharts，复杂的用 ECharts）
   - "这意味着什么"的文字解读（基于实际数据动态生成）
   - 时间范围切换（近30天/90天/全部）

【关键】
- 性能：复杂的统计算法用 SQL 完成（聚合在数据库层）
- 准确性：所有计算逻辑必须有单元测试
- 移动端：图表必须能在手机上正常显示

【数据准备】
为了让仪表盘有意义，请先创建一个 seed 脚本：
- scripts/seed-demo-data.ts
- 生成 50 条模拟决策卡（带各种情绪/盈亏/决策依据）
- 用于开发和演示

完成后让我看看 seed 数据下的仪表盘截图（描述也行）。
```

### 🎯 阶段5：月度画像 + 错误类型库（预计4天）

```
开始阶段5：月度画像（PRD 模块3.5）+ 错误类型库（PRD 模块3.7）。

【月度画像】
- 数据库：monthly_portraits 表
- 月度数据卡（自动汇总）
- 三笔关键交易（最成功/最失败/最反思）
- 6大核心问题对照表（独创亮点功能）
- 体悟 + 下月改进重点

【错误类型库】
- 数据库：error_types 表 + trade_errors 关联表
- 预置 8 种常见错误类型
- 在决策卡或月度画像中可标注"这是一次错误"
- 错误回顾视图：按累计代价排序

【关联设计】
- 决策卡 → 错误类型（多对多）
- 月度画像 → 6大问题进步评分
- 仪表盘的"错误热力图"从这两个数据源生成

完成后给我做一份"用户旅程演示"：
模拟一个用户从填决策卡 → 周复盘 → 月度画像 → 看分析仪表盘的完整流程。
```

### 🎯 阶段6：认证 + 部署（预计3天）

```
开始阶段6：用户认证 + 生产部署。

【认证】
1. NextAuth.js v5 集成
2. 支持邮箱密码登录（MVP 阶段）
3. 数据隔离：每个用户只能看自己的数据
4. 中间件（middleware.ts）保护所有 (dashboard) 路由

【部署】
1. 数据库迁移到 PostgreSQL（建议用 Supabase 免费层）
2. 部署到 Vercel
3. 环境变量配置：
   - DATABASE_URL
   - NEXTAUTH_SECRET
   - NEXTAUTH_URL
4. 配置自定义域名（可选）

【上线前 Checklist】
- [ ] 所有 API 都有用户认证检查
- [ ] 数据库索引完整
- [ ] Lighthouse Performance > 90
- [ ] 移动端在 iOS Safari / Android Chrome 测试通过
- [ ] 错误日志接入（Sentry 免费层）

完成后给我访问 URL，我会做最终验收。
```

### 🎯 阶段7：行情数据接入（预计3天）

```
开始阶段7：A股行情数据接入。

【目标】
让"现价"字段自动更新，不再依赖手动输入。

【任务】
1. 选型：
   - 免费：东方财富爬虫（需注意稳定性和合规）
   - 付费：Tushare Pro（学生免费，专业版便宜）
   先用免费方案，预留付费接口
2. 在 lib/market-data/ 创建抽象层
3. 缓存策略：Redis（或内存缓存）+ 每分钟更新
4. 持仓页的现价自动更新
5. 决策卡创建时自动带出当前价

【风险】
不要做高频调用（避免被封 IP）。
盘中可以5分钟刷新一次，盘后不刷新。

完成后告诉我数据源的稳定性测试结果。
```

---

## 📄 文件4：每日推进话术模板

> 在每个阶段中，可能需要多次对话。下面是一些常用话术。

### 检查进度
```
请向我汇报当前阶段的进度：
1. 已完成的任务（精确到子任务）
2. 进行中的任务
3. 遇到的问题或需要我确认的地方
4. 预计还需多少时间完成本阶段
```

### 代码 Review
```
请对刚才写的 [模块名] 做一次自我 Review：
1. 有哪些可以更简洁的地方？
2. 有哪些可能的 bug 或边界 case 没考虑？
3. 性能是否有问题？
4. 测试覆盖率如何？

直接告诉我，不要美化。
```

### 决策讨论
```
关于 [具体问题]，我倾向于 [方案A]，但又担心 [具体顾虑]。
请告诉我：
1. 我的顾虑是否成立？
2. 是否有更好的方案 B？
3. 给我一个明确的建议。

不要给我5个选项让我自己选，给我你的判断。
```

### 调试
```
[贴上错误信息]

请：
1. 解释这个错误的根本原因（不是表面原因）
2. 提出修复方案
3. 修复后，告诉我如何避免类似错误再次发生

不要直接改代码，先解释清楚。
```

### 结束本次对话
```
今天先到这里。请：
1. 总结今天完成了什么
2. 把未完成的任务记录到 docs/todo.md
3. 把今天学到的项目知识更新到 CLAUDE.md（如有）
4. 提交一个 git commit（带详细的 commit message）

明天我会用新的对话从你的总结继续。
```

---

## 🎯 整体使用策略

### 节奏建议
- 总周期：**3个月完成 MVP**（阶段0-3）
- 每阶段：**一次完整对话** 或 **跨多个对话但用同一个 git 分支**
- 每天：**最多1个子任务**（防止上下文爆炸）
- 周末：**review 一周代码** + 更新 CLAUDE.md

### 关键技巧

**1. 善用 `@文件名` 引用**
- 不要把 PRD 内容复制粘贴，用 `@PRD_交易日志Web应用_TradeMirror.md` 引用
- Claude Code 会自动加载文件内容

**2. 善用 `/clear` 清空上下文**
- 上下文超过一半时主动 `/clear`
- 长项目尤其重要，否则后期质量明显下降

**3. 善用计划模式（Plan Mode）**
- 复杂任务先 `Shift+Tab` 进入计划模式
- 让 Claude 先出方案，确认后再执行

**4. 善用 git 频繁提交**
- 每完成一个子任务就 commit
- commit message 详细，方便后续 review

**5. 把 Claude 学到的东西沉淀回 CLAUDE.md**
- 每次 Claude 犯错或学到新模式，立刻更新到 CLAUDE.md
- 这样下次会话不会重复犯错

### 失败信号（出现就要警惕）

- ⚠️ Claude 开始重复犯同样的错 → 上下文太长，`/clear`
- ⚠️ Claude 开始引入不必要的依赖 → 提醒 CLAUDE.md 中的"简单胜过复杂"
- ⚠️ Claude 想"重构所有代码" → 拒绝，让它专注当前任务
- ⚠️ Claude 不敢做决定，反复让你选 → 给它更明确的边界
- ⚠️ Claude 生成的代码自己都没运行过 → 让它必须先跑 build / test

---

## 💰 成本预估

如果你是订阅 Claude Pro / Max 用户，使用 Claude Code 是包含在订阅里的（有使用配额）。

如果按 API 计费：
- 一个 MVP 项目（约 3 个月，每天 2 小时）：**预估 $50-200**
- 主要看你是否经常 `/clear`（清空上下文能省钱）

---

## 📞 最后建议

**1. 不要追求完美启动**
- 先让阶段0跑起来，再说阶段1
- 看到首页能加载，比看完所有文档更有动力

**2. 不要试图一次性把所有阶段都讲完**
- 一次对话 = 一个聚焦目标
- 上下文管理是 Claude Code 用得好不好的核心

**3. 把这个项目当成 Claude 教你写代码的过程**
- 不只是"让 Claude 帮我写"，而是"和 Claude 一起写"
- 不懂的代码一定要让它解释清楚
- 你自己也要能维护，不能完全黑箱

**4. 记得做版本控制**
- 每完成一个阶段开一个 git tag（v0.1, v0.2...）
- Vercel 部署用 preview branch 方便回滚

**5. MVP 完成后再考虑商业化**
- 先自己用 1 个月，跑通完整循环
- 再邀请 3-5 个朋友试用
- 验证有人愿意付费后，再考虑做大

---

祝你顺利。如果中间遇到 Claude Code 用法问题，随时回来问我。

**记住**：你正在做的事不是"做一个产品"，是**把过去 6 年的痛苦经验，凝固成一面镜子，照见自己也照见同类人**。

这件事的价值，远超过这个产品本身的商业价值。

———

**附录：关键文档清单**

启动前请确保项目根目录有以下文件：
- [ ] CLAUDE.md（文件1的内容）
- [ ] PRD_交易日志Web应用_TradeMirror.md（之前生成的 PRD）
- [ ] .gitignore（标准 Next.js 模板）
- [ ] .env.example（环境变量示例）
