# DevLog — 2025-05-15

## 完成内容

### Phase 2: 管理员面板
- `/admin/users` 用户管理页：查看所有用户、切换角色、禁用/启用
- `/admin/users/[id]` 只读查看任意用户数据（情绪三维、操作分布、最近决策）
- `/admin/stats` 全局统计：注册数、活跃用户、本周决策、高危占比、12周趋势图
- Admin API: `PATCH /api/admin/users/[id]/role`、`/disable`
- 侧边栏 Admin 区域（仅管理员可见）
- 强制改密流程：初始 admin 账号首次登录跳转 `/change-password`

### Phase 3: Google 登录
- `auth.ts` 添加 GoogleProvider + allowDangerousEmailAccountLinking
- 登录页新增 Google 按钮（含官方 SVG logo）
- OAuth 用户跳过强制改密
- 同邮箱账号自动合并

### 股票自动补全
- `GET /api/stocks/search?q=xxx` 服务端代理（东方财富 suggest API + 用户历史数据）
- `StockCombobox` 组件：防抖搜索、键盘导航、代码/名称双向匹配
- 决策表单 + 持仓表单集成，选中自动填入代码、名称、市场

### 决策卡日期字段
- Step 1 新增「交易时间」datetime-local 选择器
- 留空默认当前时间，支持补录历史交易
- validator 新增可选 `tradedAt`，API 使用 `tradedAt || Date.now()`

### UI 优化
- 侧边栏移除底部用户信息（与右上角重复）
- 右上角保留用户头像下拉菜单

## 技术决策
- 股票搜索用东方财富 suggest API（免费、无鉴权、<100ms）
- 管理员查看用户数据复用现有 query 函数（传 targetUserId）
- JWT 中增加 passwordChangedAt 判断是否需要强制改密
- Google OAuth 用 allowDangerousEmailAccountLinking 实现同邮箱合并
